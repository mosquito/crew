machine:
  services:
    - rabbitmq-server
  python:
      version: 2.7.5

dependencies:
  override:
    - pip install wheel
    - pip install tornado nose requests
    - pip install -e .

test:
  override:
    - nosetests -v -d tests/*